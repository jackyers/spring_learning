server:
  port: 7001

#配置eureka服务端
eureka:
  client:
    register-with-eureka: false #禁止自己注册自己
    fetch-registry: false #禁止拉取注册中心的服务信息
    service-url:
      defaultZone: http://localhost:7002/eureka/ #eureka服务端的地址